// Package acceptance holds hidden tests for the benchmark grader.
// grade.sh rewrites the replace directive to point at each run artifact.
package acceptance_test

import "testing"

// TestSmoke is a placeholder until you add tests that import the candidate module.
// grade.sh also runs go build and go vet on the artifact directory.
func TestSmoke(t *testing.T) {
	t.Log("add acceptance tests that import your module packages")
}
